<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ext="clr-namespace:SimpleQ.Extensions"
             x:Class="SimpleQ.Pages.SettingsPage"
             Title="{ext:Translate Text=Settings}">
    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding MenuItems}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal">
                                <Image Source="{Binding IconResourceName}" VerticalOptions="Center"></Image>
                                <Label Text="{Binding Title}" Style="{DynamicResource BodyStyle}" VerticalOptions="Center"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Command="{Binding LogOutCommand}" Text="{ext:Translate Text=LogOut}"></Button>
            <Button Command="{Binding JoinDepartmentCommand}" Text="{ext:Translate Text=JoinDepartment}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>