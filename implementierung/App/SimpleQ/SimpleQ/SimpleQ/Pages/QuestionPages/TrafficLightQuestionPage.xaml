<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SimpleQ.Pages.QuestionPages.TrafficLightQuestionPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding Question.CatName, Mode=TwoWay}" Style="{DynamicResource CaptionStyle}"></Label>
            <Label Text="{Binding Question.SurveyDesc, Mode=TwoWay}" Style="{DynamicResource BodyStyle}"></Label>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="4*"></ColumnDefinition>
                    <ColumnDefinition Width="4*"></ColumnDefinition>
                    <ColumnDefinition Width="4*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="32"></RowDefinition>
                </Grid.RowDefinitions>
                <Button Grid.Column="0" Command="{Binding GreenCommand}" BackgroundColor="Green"></Button>
                <Button Grid.Column="1" Command="{Binding YellowCommand}" BackgroundColor="Yellow"></Button>
                <Button Grid.Column="2" Command="{Binding RedCommand}" BackgroundColor="Red"></Button>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>